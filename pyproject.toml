[project]
name = "stalleymod-harness"
version = "0.1.0"
description = "Fixture-driven testing and debugging harness for Stalley Mod"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.116.0,<1",
    "pyyaml>=6.0.2,<7",
    "uvicorn>=0.35.0,<1",
]

[dependency-groups]
dev = [
    "httpx2>=2.9.0,<3",
]
